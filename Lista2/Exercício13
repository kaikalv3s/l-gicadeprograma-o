#include <iostream>
#include <cmath>
using namespace std;

int main() {
    float a, b, c, delta;
    cin >> a >> b >> c;

    delta = b*b - 4*a*c;

    if (delta < 0)
        cout << "Sem raiz";
    else {
        float x1 = (-b + sqrt(delta)) / (2*a);
        float x2 = (-b - sqrt(delta)) / (2*a);
        cout << x1 << " " << x2;
    }

    return 0;
}
