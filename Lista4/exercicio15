#include <iostream>
using namespace std;

void troca(int &a, int &b){
    int temp = a;
    a = b;
    b = temp;
}

int main(){
    int a, b;

    cout << "Digite dois numeros: ";
    cin >> a >> b;

    troca(a, b);

    cout << "Apos troca: a = " << a << " b = " << b;

    return 0;
}
