#include <iostream>
#include <string>
using namespace std;

int main() {
    string mes, abr;
    int i=0;
    cout << "Digite um mês: ";
    getline(cin, mes);

    for (int i = 0; i < 3; i++) {
        abr += mes[i];
       
    }
    i=3;
 if(i==3){
            abr+='.';
 }
    cout << abr;

    return 0;
}
